import React from 'react';
import Header from '../Header/Header';
import Menu from '../Menu/Menu';

const Home = () => {
    return (
        <div>
            <Menu></Menu>
        </div>
    );
};

export default Home;